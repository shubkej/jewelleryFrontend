import { BoxProps } from "@mui/material";

const LoadingComponentsStyle = {
  mainBox: {
    sx: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
    },
  } as BoxProps,
  containerBox: {
    sx: { height: 40 },
  } as BoxProps,
};

export default LoadingComponentsStyle;
